# 交接：真实验收阶段进展与手机端卡点

> 记录时间：2026-09-01 11:21（Asia/Shanghai）
> 上游文档：`交接指南.md`（主交接文档，先读它）、`待用户处理.md`（11 步验收清单）
> 本文档性质：真实端到端验收进行到第 4 步（手机配对）时卡住，供下一个会话/其他模型接手

---

## 1. 一句话现状

公网链路已全线打通（手机能打开电脑上的配对页面），卡在最后一步：**手机的 WebAuthn 平台认证器系统级不可用**，任何浏览器都弹不出「此设备」选项，Passkey 无法创建。服务端代码已核实无嫌疑。

## 2. 已完成的事项

### 2.1 代码与仓库（分支 `feat/secure-passkey-access`，均未推送）

- `06ecaf0` feat: auto-download official cloudflared with sha256 verification
  - `lib/tunnel.mjs`：cloudflared 解析顺序 = 显式路径（缺失仍失败关闭）→ PATH → 官方 GitHub Releases 下载 + SHA-256 校验（摘要取自 api.github.com 资产 `digest` 字段，无第三方镜像）；受管副本存 `$DSH_HOME/dsh-pocket/bin/`
  - 自动测试 74/74 通过；真实下载实测通过（见 2.3）
- `e616872` docs: move original design doc into project docs
  - 设计文档已迁入 `docs\dsh-pocket安全改造设计.md`
- SECURITY.md / README.md / CHANGELOG.md / 交接指南.md / 待用户处理.md 已同步上述变更

### 2.2 插件部署

- 已通过 `dsh plugin --profile desktop add D:\Github\dsh-pocket` 安装进 desktop profile
- pnpm 以 `link:` 协议接入：`node_modules\dsh-pocket` 是指向 `D:\Github\dsh-pocket` 的 Junction，**源码改动即时生效，无需重装**
- DSH Desktop 已重启并加载插件，设置页「手机访问」可用
- 安装日志仅有一个 cordis peer 依赖提醒（与其他插件同款，无碍）

### 2.3 cloudflared

- 插件自动下载在本机直连时曾卡死（0 字节挂 2 分钟以上，600 秒超时）；后改走系统代理手动下载成功
- 受管副本已就位：`C:\Users\Zinger\.dsh\dsh-pocket\bin\cloudflared.exe`，版本 2026.8.3，54,841,128 字节，SHA-256 与官方 API 摘要一致（`83e726ed…d14eaae`），`--version` 试运行正常
- 因此下次启动隧道会跳过下载，直接进「启动隧道进程」

### 2.4 Cloudflare 与隧道（用户操作，已完成）

- Cloudflare 账号：864271839@qq.com（Zero Trust Free，team name `restless-darkness-6c5a`）
- Named Tunnel 已创建；Public Hostname 已配置：
  - 固定域名：`dsh-pocket-addin-work.zingerplayground.top`（zingerplayground.top 托管在 Cloudflare）
  - Service：`http://127.0.0.1:3081`
- settings.json（`~\.dsh\dsh-pocket\settings.json`）已写入 hostname 和 tunnelToken（Token 不落文档）
- **隧道已就绪**：手机手机网络下能打开 `https://dsh-pocket-addin-work.zingerplayground.top/pocket-pair` 配对页——说明 DNS、证书、Tunnel、代理、配对页全链路正常。这相当于 11 步验收清单的第 1-2 步已事实通过。

## 3. 卡点：手机 WebAuthn 平台认证器不可用

### 3.1 现象

- 配对页正常渲染，设备名可填（用户填了 DIKA-phone）
- 点「确认手机身份」后 WebAuthn 弹窗出现，rpId 显示正确（`dsh-pocket-addin-work.zingerplayground.top`），但选项只有：
  - NFC 安全密钥
  - USB 安全密钥
  - 改用另一部设备
  - **没有「此设备」（本机指纹/锁屏）选项**
- 继续确认报错：`An unknown error occurred while talking to the credential manager.`
- **Edge 与 Chrome 表现完全一致**

### 3.2 关键定位证据（切割测试）

- 用手机 Chrome 打开参考站 `passkeys.io` 注册通行密钥：**同样没有「此设备」选项**（同样只给 NFC/USB/另一部设备）
- 结论：**与本项目服务端无关，也与浏览器选择无关，是手机系统级的平台认证器/凭据管理器问题**
- 补充观察：passkeys.io 的弹窗中出现过一条 Google 账号（zikebai@…，邮箱不入档）的「通行密钥」条目和「在另一部设备上创建通行密钥？」的混合流程界面，说明 Google 密码管理器有部分活性，但本机平台认证器仍不可用

### 3.3 服务端代码核实（已排除嫌疑）

`lib/device-auth.mjs` 的 `registrationOptions()`：

```js
authenticatorSelection: {
  residentKey: 'preferred',
  userVerification: 'required',
}
```

没有指定 `authenticatorAttachment`，未限制认证器类型；rpID 为固定域名本身，合法。登录侧同样只有 `userVerification: 'required'` + `allowCredentials` 白名单。

### 3.4 手机侧已知信息

- Android 手机（具体品牌/型号/Android 版本未知，用户命名为 DIKA-phone）
- 有指纹硬件，用户确认「支持指纹验证」——**但未确认是否已设置锁屏凭据（见 4.1 假设 1）**
- 有完整 Google 服务（Play 商店可用，用户确认）
- 试过的浏览器：Edge、Chrome
- 状态栏常开 VPN（判断与本问题无关，WebAuthn 凭据管理器是本地 IPC）

## 4. 剩余假设（按概率排序，均未验证）

1. **锁屏凭据未设置**：指纹「硬件支持」≠ 已配置。Android 平台认证器要求安全锁屏，没设锁屏时所有浏览器的「此设备」都会消失。→ 让用户确认 设置→安全 里的锁屏/指纹是否已启用
2. **Google Play 服务过旧或被 ROM 阉割**：Credential Manager/FIDO 实现在 Play 服务里；部分国产 ROM 即使 Play 商店可用也会阉割相关组件。→ 更新 Play 服务后重测 passkeys.io
3. **系统凭据管理器默认提供方未设置**：设置里搜「凭据/通行密钥/密码」，确认「Google 密码管理工具」已启用且为默认
4. Chrome 与 Play 服务版本组合 bug → 两者都更到最新再测

## 5. 候选处理方向（按优先级，均未实施/未决定）

- **A. 修手机侧（推荐首选）**：按 4.1→4.4 逐项检查，每改一项用 passkeys.io 重测；passkeys.io 出现「此设备」后回本项目重新扫码配对（二维码 5 分钟失效、单次使用，需在电脑重新生成）
- **B. 浏览器无关替代**：手机装 Bitwarden / 1Password 等 Passkey 管理器作为系统级凭据提供方；任何浏览器的 WebAuthn 都会委托给它。配对与登录代码零改动
- **C. 硬件安全密钥**：现有 UI 已给 NFC/USB 选项，插/NFC 触碰即可注册（需用户购买硬件，大概率不必）
- **D. 跨设备流程**：用另一部平台认证器正常的手机（如 iPhone）在「改用另一部设备」里通过混合流程创建——但日常使用仍要这台 DIKA-phone，治标不治本
- **E. 设计变更（不建议，违反已定决策）**：加 TOTP/验证码/PIN 类第二通道。直接违反交接指南第 2 节第 4 条「Passkey only」，为单台手机的故障削弱安全模型。若最终走这条路必须用户明确拍板，并在文档中标注待决定

## 6. 环境事实与坑（接手必读）

- **本机直连 GitHub 慢且不稳**：Node fetch 不走 Windows 系统代理；系统代理 `127.0.0.1:17891` 可用且快（curl 实测直连 120KB/s、代理 622KB/s）
- **插件下载单飞 + 600 秒超时**：下载卡死后重复点「开始」只会复用卡住的请求，必须等超时或重启 DSH Desktop 才能重试（体验问题，是否加「取消进行中下载」待议，未动代码）
- **PowerShell 噪声**：本仓库环境跑 pwsh 会反复输出 `Cannot create type. Only core types are supported in this language mode.`，是受限语言环境噪声，非项目错误
- **多 AI 协作纪律**：任何提交前后查 `git branch --show-current` / `git status --short`；不推送、不开 PR、不发布、不部署
- **不要做的事**：不恢复 LAN/PIN/Quick Tunnel/无认证 CLI/第三方镜像下载；不改交接指南第 2 节 15 条已定决策；不把 Token/密码写进仓库
- **client 产物**：`client/client.js`、`client/passkey.js` 是构建产物，改 `client/index.jsx`、`client/passkey-entry.js`、`client/pocket-locales.js` 后必须 `npm run build:client`
- **验收清单**：剩余 11 步清单在 `待用户处理.md` 第 4 节；目前事实完成第 1-2 步，卡在第 3 步（手机扫码配对）

## 7. 关键路径速查

```text
源码            D:\Github\dsh-pocket
受管 cloudflared C:\Users\Zinger\.dsh\dsh-pocket\bin\cloudflared.exe
插件设置         C:\Users\Zinger\.dsh\dsh-pocket\settings.json
固定域名         https://dsh-pocket-addin-work.zingerplayground.top
本地代理         http://127.0.0.1:3081
设计文档         docs\dsh-pocket安全改造设计.md
```
