# 待用户处理

以下事项需要 Cloudflare 账号、真实手机或当前 DSH Desktop，由用户操作后才能完成最终验收。代码侧独立工作不依赖这些步骤。

## 1. Cloudflare 固定入口

- 在 Cloudflare Zero Trust 创建 Named Tunnel。
- 选择一个固定 Hostname，例如 `pocket.example.com`。
- 把该 Hostname 的 Service 指向实际代理端口，默认：`http://127.0.0.1:3081`。
- 复制 Tunnel Token；不要提供 Cloudflare 账号密码。

## 2. 官方 cloudflared

- 从 Cloudflare 官方渠道安装 `cloudflared` 并加入 `PATH`；或准备官方二进制绝对路径。
- 本分支不会从 GitHub 加速站或第三方镜像自动下载可执行文件。

## 3. DSH Desktop 条件

- 确认 DSH Desktop 已开启「浏览器访问」。
- 使用 `compatibility` 模式；`advanced` 模式缺少手机网页布局。

## 4. 真实设备验收

按顺序验证：

1. 本机设置固定域名和 Tunnel Token，开启公网入口。
2. 电脑生成配对二维码。
3. 手机扫码，填写设备名称，完成人脸/指纹/设备 PIN。
4. 确认手机此时仍不能进入 DSH。
5. 电脑本机点击允许。
6. 手机打开固定网址，完成 Passkey 后进入 DSH。
7. 刷新页面，确认再次要求 Passkey。
8. 页面保持前台超过 10 分钟，确认持续可用。
9. 页面转后台超过 10 分钟，确认返回后需重新验证。
10. 电脑撤销设备，确认手机现有页面和 WebSocket 在约 30 秒内失效。
11. 重启 DSH，确认旧会话失效，但已批准 Passkey 凭据仍存在。

## 5. 发布前决定

- 是否把安全改造作为独立 fork 发布，还是仅向上游提交设计/PR。
- 若独立发布，需要修改包名、版本、仓库元数据和发布流程，避免覆盖上游 `dsh-pocket` npm 包。
- 当前没有推送分支、创建 PR、发布 npm 包或安装到正在使用的 DSH profile。
