# 待用户处理

以下事项需要 Cloudflare 账号、真实手机或当前 DSH Desktop，由用户操作后才能完成最终验收。代码侧实现和自动测试已经完成。

## 1. Cloudflare 固定入口

当前公司电脑环境已经配置：

- 固定 Hostname：`dsh-pocket-addin-work.zingerplayground.top`；
- Service：`http://127.0.0.1:3081`；
- Named Tunnel 与手机网络访问配对页已通过旧方案真实验证。

家里电脑使用时需要单独创建 Named Tunnel、固定 Hostname 和 Tunnel Token；两台电脑不共享设备或会话。

## 2. 官方 cloudflared

- 公司电脑的受管 `cloudflared` 已下载并校验完成。
- 家里电脑可手动安装官方版本，也可让插件从 Cloudflare 官方 GitHub Releases 下载并校验 SHA-256。
- 受管副本位于 `$DSH_HOME/dsh-pocket/bin/`。

## 3. DSH Desktop 条件

- 确认 DSH Desktop 已开启「浏览器访问」。
- 使用 `compatibility` 模式；`advanced` 模式缺少手机网页布局。
- 当前源码通过 `link:`/Junction 接入正在使用的 Desktop profile；重新验收前需要在用户确认后重启 DSH Desktop，使新后端代码和客户端产物重新加载。

## 4. 荣耀 50 真实设备验收

部署与重启 DSH Desktop 后，按顺序验证：

1. 电脑设置页能正常显示固定入口和“允许访问的设备”。
2. 电脑生成配对二维码。
3. 荣耀 50 使用目标浏览器扫码，填写设备名称以及至少 6 个字符的密码，并再次确认密码。
4. 手机提交申请后确认此时仍不能进入 DSH；保留配对页。
5. 电脑本机显示设备名称和申请时间，点击“允许”。
6. 返回手机配对页，点击“电脑已批准，进入 DSH”，输入设备密码后进入 DSH。
7. 正常刷新页面，确认不重复输入密码。
8. 页面保持前台超过 10 分钟，确认持续可用。
9. 切到后台不足 10 分钟后返回，确认不重复输入密码。
10. 切到后台超过 10 分钟后返回，确认要求重新输入密码。
11. 使用同一手机的另一个浏览器打开固定网址，确认显示“此浏览器尚未配对”，即使知道密码也不能登录。
12. 电脑撤销设备，确认手机现有 HTTP/API 和 WebSocket 会话在约 30 秒内失效。
13. 重新配对后重启 DSH，确认旧登录会话失效，但已批准设备仍存在并可输入密码登录。
14. 删除浏览器 Cookie 或清除浏览数据，确认必须回电脑重新配对。
15. 公网入口开启状态下重启 DSH Desktop，确认公网自动恢复开启。
16. 手动关闭公网入口后重启 DSH Desktop，确认保持关闭。

真实验收时不要在聊天、截图或仓库中记录设备密码、Tunnel Token 或原始 Cookie。

## 5. 发布前决定

- 是否把安全改造作为独立 fork 发布，还是仅向上游提交设计/PR。
- 若独立发布，需要修改包名、版本、仓库元数据和发布流程，避免覆盖上游 `dsh-pocket` npm 包。
- 当前功能分支仍未推送、未创建 PR、未发布 npm 包；新版认证代码也尚未重新加载到正在运行的 DSH Desktop。
