# DSH Pocket 交接指南

> 交接时间：2026-09-01 15:35（Asia/Shanghai）
> 当前阶段：新认证主体已提交；配对引导优化完成、待实际页面验证
> 源码唯一位置：`D:\Github\dsh-pocket`
> 当前分支：`feat/secure-passkey-access`
> 本文件为常驻交接文档，每次覆盖更新

## 0. 新会话先做什么

1. 使用 `D:\Github\dsh-pocket`，不要复制源码。
2. 先阅读本文件、`docs\dsh-pocket安全改造设计.md`、`README.md`、`SECURITY.md`、`待用户处理.md`。
3. `交接-真实验收阶段-20260901-1121.md` 只作为旧 Passkey 方案在荣耀 50 上失败的历史证据，不再代表当前方案。
4. 同步并核对仓库，但不要改变工作树：

   ```powershell
   git fetch --all --prune
   git branch --show-current
   git status --short
   git log -5 --oneline --decorate
   git remote -v
   ```

5. 已提交：

   ```text
   ef3dd4a docs: redesign authentication for China Android
   971f9a5 feat: replace passkeys with device passwords
   ```

6. 当前未提交改动只涉及配对流程说明、手机配对完成页入口按钮及对应测试/文档。不要推送、部署、发布或重启 DSH Desktop，除非用户明确确认。

## 1. 当前目标与验收边界

- 本轮先满足用户本人使用：荣耀 50 通过纯浏览器访问公司与家里两台电脑。
- 不依赖 Google Credential Manager、WebAuthn、短信、邮箱、国外身份服务或手机 App。
- 当前不对所有国产 ROM、中国大陆运营商网络或普通中国用户作普适性承诺。
- 固定 HTTPS 域名、Cloudflare Named Tunnel、代理仅监听 `127.0.0.1` 的网络边界保持不变。
- 公司电脑的公网链路已在旧方案实测打通；新版认证尚未重新部署和实测。

## 2. 已确认的业务流程

### 2.1 首次配对

1. 只能在电脑本机点击“添加设备”。
2. 电脑显示 5 分钟有效、一次性的二维码。
3. 目标手机浏览器填写设备名称、设备密码并再次确认。
4. 密码允许数字、字母和符号，长度 6–256 个字符。
5. 手机提交后仍不能访问 DSH；可以保留配对页，也可以稍后重新打开。
6. 电脑显示设备名称和申请时间，用户点击“允许”后才加入白名单。
7. 返回手机配对页，点击“电脑已批准，进入 DSH”，再输入刚设置的设备密码。
8. 同一时间只允许一个待批准申请；已有申请时拒绝新申请并保留原申请。
9. 同一手机的不同浏览器分别配对；清除浏览数据或更换浏览器后重新配对。

### 2.2 日常登录

- 登录同时要求浏览器持有电脑批准过的随机设备 token，并输入该设备的 Pocket 密码。
- 正常刷新和持续前台使用复用短会话。
- 关闭页面、切后台或锁屏都会停止前台续期；连续无前台活动满 10 分钟后要求重新输入密码。
- 网页无法可靠识别“真正关闭浏览器”，不要承诺关闭后立即失效。
- 页面明确不建议保存密码并尽量抑制自动填充，但浏览器可能不遵守。

### 2.3 密码失败与恢复

- 第 1–4 次失败立即重试；第 5 次等待 5 分钟；第 10 次等待 15 分钟；第 15 次等待 45 分钟；第 20 次及以后每累计 5 次等待 24 小时。
- 失败次数和等待截止时间持久化；等待期间尝试不继续累计；正确登录后清零。
- 不提供改密、重置、邮箱、短信、验证码或恢复码。
- 忘记密码或想换密码时，只能在电脑删除设备并重新配对。

## 3. 已完成实现

### 3.1 服务端认证

`lib/device-auth.mjs` 已替换原 SimpleWebAuthn 模型：

- 长期设备 Cookie：`__Host-dsh_pocket_device`；随机 256 位，服务端只保存 SHA-256 哈希，完整登录后轮换，`Max-Age=365` 天。
- 短会话 Cookie：`__Host-dsh_pocket_session`；无长期 `Max-Age`，会话只在内存。
- 密码：`argon2@0.45.1`，Argon2id `m=19456 KiB, t=2, p=1`；包内含 Windows x64 和 Linux glibc/musl 预编译文件。
- 未知设备也执行等价 dummy Argon2 验证；登录错误统一。
- 登录端点按来源 30 次/分钟，配对提交按来源 10 次/5 分钟。
- 旧 `version: 1` Passkey 状态不迁移，失败关闭并要求重新配对。
- 状态文件仍为 `$DSH_HOME/dsh-pocket/device-credentials.json`，新格式为 `version: 2`。

### 3.2 代理与页面

- `lib/proxy.mjs` 不再要求每次刷新消耗 `pocket-entry` 票据；有效短会话可正常刷新。
- 未登录导航按浏览器状态显示“尚未配对”或密码登录页；API/WS 返回 401。
- `client/passkey-entry.js` / `client/passkey.js` 已迁移为 `client/auth-entry.js` / `client/auth.js`。
- 未登录 bundle 不再包含 SimpleWebAuthn 或 `navigator.credentials`；脚本 URL 改为 `/pocket-auth/auth-v2.js` 并使用 `Cache-Control: no-store`，绕过荣耀 50 已缓存的一年期旧 Passkey 脚本。
- 配对页提供设备名、密码和确认密码；提交后提示可关闭页面。
- 设置页使用 `devices/deviceCount`，不再显示 Passkey 同步信息。
- 认证页面设置 CSP、HSTS、禁止 iframe，并关闭摄像头、麦克风和定位权限。

### 3.3 依赖与测试

- 已移除 `@simplewebauthn/browser` 和 `@simplewebauthn/server`。
- 已固定 `argon2@0.45.1`。
- `test/device-auth.test.js` 覆盖设备 token 哈希、轮换、Argon2 抽象、一次性配对、单待批准申请、失败等待持久化、旧状态失败关闭、安全 Cookie 和公开端点限速。
- `test/secure-proxy.test.js` 覆盖未配对提示、密码页、刷新复用会话、HTTP/API/WS 拒绝与放行。
- `test/smoke.test.js` 验证认证 bundle 不含 WebAuthn。

## 4. 当前文件状态

认证主体已提交到 `971f9a5`。当前未提交改动为：

- 电脑设置页改为完整的扫码、提交、批准、返回手机说明；
- 手机配对完成页新增“电脑已批准，进入 DSH”根路径按钮；
- 更新 `README.md`、设计文档、验收清单和本交接指南；
- `test/secure-proxy.test.js` 增加按钮文案和 `href="/"` 检查；
- 重新生成 `client/client.js` 和 `client/auth.js`。

## 5. 最终检查与真实验收

2026-09-01 15:06 已完成：

- `npm run build:client`：通过；
- `npm test`：79/79 通过；
- `npm audit --omit=dev`：0 个生产依赖漏洞；
- `npm pack --dry-run --json`：通过，33 个文件，约 119 KB；
- `git diff --check`：通过。

提交代码前如有继续修改，重新执行：

```powershell
npm run build:client
npm test
npm audit --omit=dev
npm pack --dry-run --json
git diff --check
```

然后检查：

```powershell
git branch --show-current
git status --short
git diff --stat
git diff -- package.json package-lock.json
git grep -n -E "Passkey|WebAuthn|passkey|pocket-entry" -- ':!交接-真实验收阶段-20260901-1121.md' ':!docs/dsh-pocket安全改造设计.md' ':!交接指南.md' ':!test/device-auth.test.js' ':!test/smoke.test.js'
```

重新部署或重启 DSH Desktop 前必须向用户说明并获得明确确认。真实验收步骤见 `待用户处理.md`。

## 6. 已知安全边界

1. 通过认证的手机拥有完整 DSH 能力，相当于可以操作主机上的 DSH 权限。
2. “Pocket 设置仅本机显示”不是密码学权限边界；DSH RPC 的 loopback authority 无法区分代理后的远程请求。
3. 纯浏览器方案无法防御同源 XSS、root/恶意软件、完整浏览器 profile 复制、已失陷电脑或 Cloudflare 账号。
4. `HttpOnly` 设备 Cookie 是 bearer 凭据；设备密码、Argon2 和失败等待是第二层保护。
5. 6 位纯数字密码在服务器凭据文件泄露时仍可能被离线穷举；Argon2 只能提高成本。
6. 按来源限速使用 Cloudflare/代理来源头；它降低洪泛成本，但不构成身份认证。

## 7. 不要做的事情

- 不要恢复 Passkey 作为并列备用认证。
- 不要恢复 LAN、旧 PIN、`?token=`、30 天登录、Quick Tunnel、无认证 CLI 或第三方镜像。
- 不要创建用户名账号系统、邮箱/短信找回或远程恢复旁路。
- 不要保存、显示或记录明文设备密码和原始设备 token。
- 不要自动迁移旧 Passkey 凭据。
- 不要运行 `npm audit fix --force`。
- 不要擅自推送、发布、部署或重启正在使用的 DSH profile。
