# DSH Pocket 交接指南

> 交接时间：2026-09-01 14:15（Asia/Shanghai）
> 当前阶段：认证方案已经重新设计，下一会话开始改造代码
> 源码唯一位置：`D:\Github\dsh-pocket`
> 当前分支：`feat/secure-passkey-access`
> 本文件为常驻交接文档，每次覆盖更新，不再新增并列交接文件

## 0. 新会话先做什么

1. 工作目录使用 `D:\Github\dsh-pocket`，不要复制源码到其他目录。
2. 先完整阅读：
   - 本文件；
   - `docs\dsh-pocket安全改造设计.md`（新认证方案的唯一权威设计）；
   - `README.md`、`SECURITY.md`、`待用户处理.md`（注意：这三份目前仍描述旧 Passkey 实现，代码改造完成后必须同步更新）；
   - `交接-真实验收阶段-20260901-1121.md`（旧 Passkey 方案在荣耀 50 上失败的实测证据，作为历史背景阅读）。
3. 按用户 GitHub 协作规则同步远端信息，但不要改变工作树：

   ```powershell
   git fetch --all --prune
   git branch --show-current
   git status --short
   git log -5 --oneline --decorate
   git remote -v
   ```

4. 应看到：
   - 当前分支 `feat/secure-passkey-access`；
   - `HEAD` 为 `073dcd5 docs: add acceptance-phase handover (phone passkey blocker)`；
   - `docs/dsh-pocket安全改造设计.md` 有本会话未提交修改；
   - `交接指南.md` 也有未提交修改；
   - 当前功能分支尚未推送到 `origin`。
5. 不要推送、开 PR、发布 npm、部署或提交，除非用户明确授权。
6. 用户已明确：只与用户讨论业务和人类使用流程；Cookie、哈希算法、参数、接口和代码结构等技术细节由 AI 按业界常用方案决定，不再逐项询问用户。

---

## 1. 为什么必须改方案

当前代码已经完整实现 Passkey/WebAuthn，但真实验收发现它不满足中国用户的通用性要求：

- 荣耀 50 手机扫码能正常打开配对页；
- Chrome 与 Edge 的 WebAuthn 弹窗都只有 NFC、USB 和“改用另一部设备”，没有本机认证器；
- 参考网站 `passkeys.io` 在同一手机上也失败，证明不是项目参数或服务端实现错误，而是国产 ROM/Google Credential Manager 环境不提供浏览器平台认证器；
- 银行 App 可以使用指纹，不代表网页也能调用指纹。银行 App 使用原生 Android 生物识别 API；普通网页只能经 WebAuthn 间接调用系统认证器。

因此结论是：

- 不能通过完善现有 Passkey 参数让这台手机弹出“此设备”；
- 如果插件必须让中国普通 Android 用户使用，`Passkey only` 产品方案不合格；
- 下一阶段应替换认证方案，而不是继续排查荣耀手机或增加 Google 服务依赖。

公网链路本身已经通过真实验证：固定域名、Cloudflare Named Tunnel、HTTPS、loopback 代理和手机访问配对页均正常。

---

## 2. 已确认的业务流程（不要重新询问）

### 2.1 硬约束

1. 手机端必须是纯浏览器，不安装手机 App。
2. 必须覆盖中国 Android/国产 ROM，不依赖 Google Credential Manager、WebAuthn、短信、邮箱或国外身份服务。
3. 安全目标高于认证形式；不强制指纹或手机系统 PIN。
4. 手机已经解锁仍不够，进入 DSH 前必须完成一次 Pocket 独立认证，体验类似银行 App 的独立登录。
5. 固定 HTTPS 域名、Cloudflare Named Tunnel、代理仅监听 `127.0.0.1` 等现有网络安全边界保持不变。

### 2.2 首次配对

1. 只能在电脑本机点击“添加设备”。
2. 电脑显示 5 分钟有效、一次性的配对二维码。
3. 手机扫码后填写：
   - 设备名称；
   - 自定义设备密码，至少 6 位。
4. 手机提交申请后仍然不能进入 DSH。
5. 电脑本机显示设备名和提交时间，用户点击“允许”后才加入设备白名单。
6. 每个浏览器实例是一台独立设备：同一手机的 Chrome、Edge 需要分别配对；清除浏览数据或更换浏览器后重新配对。

### 2.3 日常登录

1. 登录同时要求：
   - 浏览器持有电脑批准过的设备凭据；
   - 用户输入该设备自己的 Pocket 密码。
2. 每台设备独立设置密码；系统不强制多设备共用。
3. 以下情况要求重新输入密码：
   - 首次进入；
   - 新会话；
   - 页面退到后台超过 10 分钟后返回。
4. 页面持续在前台使用和正常刷新时不重复要求输入密码。
5. 不设置长期免登录会话。

### 2.4 输错密码

失败次数按设备累计并持久化，只有密码输入正确才清零：

- 第 1–4 次：立即允许重试；
- 第 5 次失败后：等待 5 分钟；
- 第 10 次失败后：等待 15 分钟；
- 第 15 次失败后：等待 45 分钟；
- 第 20 次及以后：每累计 5 次失败等待 24 小时。

等待截止时间与累计次数必须持久化，重启 DSH 不能绕过。用户选择了逐步等待，不采用固定次数后必须回电脑解锁的永久锁定。

### 2.5 忘记密码与撤销

- 不提供远程找回、重置原设备密码、邮箱、短信、验证码或恢复码。
- 忘记密码后，只能在电脑本机删除该设备，再按新设备完整重新配对。
- 手机丢失或浏览器不再使用时，同样在电脑本机删除设备；对应活动会话立即失效。
- 电脑端不保存、显示或回显明文密码。

---

## 3. 已确定的技术基线（由 AI 执行，不再问用户）

完整依据见 `docs\dsh-pocket安全改造设计.md`。独立攻击者视角评审比较了长期随机 Cookie 与 WebCrypto 设备密钥，结论是：在本项目“每次重认证仍需设备密码”的前提下，两者面对 XSS、手机本机特权访问、浏览器数据复制等真实攻击面等价；WebCrypto 只增加密钥生命周期、IndexedDB 和兼容性复杂度。

采用最小复杂度的业界通行方案：

1. **设备凭据**
   - 长期随机 `__Host-` Cookie；
   - CSPRNG 至少 256 位；
   - `Secure; HttpOnly; SameSite=Strict; Path=/`，不设置 `Domain`；
   - 服务端只保存 token 的 SHA-256 哈希；
   - 每次完整密码认证成功后轮换设备 token；
   - 含凭据响应设置 `Cache-Control: no-store`。
2. **密码存储**
   - Argon2id；不低于 OWASP 最低参数 `m=19MiB, t=2, p=1`；
   - 每设备独立随机盐；
   - 常数时间比较；
   - 未知设备也执行等价 dummy 哈希，避免时序泄露；
   - 错误信息统一，不区分设备不存在、密码错误或正在等待。
3. **会话**
   - 短期 `__Host-` 会话 Cookie，无长期 `Max-Age`；
   - 10 分钟失效由服务端判定，不能依赖客户端计时；
   - 页面隐藏时停止 keepalive，恢复可见时检查；
   - 登录成功重新生成会话 ID；
   - HTTP、API、WebSocket 统一验证会话，撤销设备后全部失效。
4. **配对**
   - 256 位随机 secret，5 分钟有效；
   - secret 继续放 URL fragment，不进入 Cloudflare 或服务器访问日志；
   - 首次提交即消费，无论随后批准或拒绝；
   - 同一时刻只允许一个待批准申请；
   - 电脑批准界面显示设备名与提交时间；
   - 配对提交端点独立限速。
5. **页面与传输**
   - Cloudflare 使用 Full (strict) 回源加密；
   - HSTS；
   - 认证页面收紧 CSP，禁止内联脚本和 `object-src`；
   - XSS 防护是第一优先边界。

密码下限按用户决定维持“至少 6 位”。已知残余风险：NIST 对多因子场景建议至少 8 位；如果服务器凭据数据库泄露，6 位纯数字密码即使使用 Argon2id 也可能被离线穷举。不要擅自把下限改成 8 位；可以在实现和安全文档中如实提示。

---

## 4. 当前代码仍是什么状态

代码尚未开始改造，仍是完整的 Passkey 实现：

- `lib/device-auth.mjs`：SimpleWebAuthn 注册、认证、公钥凭据、挑战、会话与撤销；
- `client/passkey-entry.js`：浏览器调用 `startRegistration()` / `startAuthentication()`；
- `client/passkey.js`：上述文件的生成产物；
- `lib/auth-pages.mjs`：Passkey 登录页、配对页和会话守卫；
- `lib/proxy.mjs`：加载 `client/passkey.js`，使用 Passkey 设备会话保护 HTTP/WS；
- `client/index.jsx`：电脑本机的 Passkey 设备管理界面；
- `package.json` / `package-lock.json`：仍依赖 `@simplewebauthn/browser` 和 `@simplewebauthn/server`；
- `test/device-auth.test.js`、`test/secure-proxy.test.js`、`test/smoke.test.js` 等仍验证 Passkey 流程。

`README.md`、`SECURITY.md`、`待用户处理.md`、`CHANGELOG.md` 和主交接以外的旧验收记录也仍描述 Passkey。这是因为运行代码尚未改变，并非遗漏；改造代码时必须同步更新。

旧代码最后一次完整验证结果：

```text
npm run build:client
npm test

74 tests
74 pass
0 fail
```

这只是旧 Passkey 实现的基线，不代表新密码方案已经测试。

---

## 5. 下一会话的实现任务

按以下顺序推进，不要先改 UI 再补安全模型：

### 第一阶段：替换服务端设备认证模型

1. 先读 `lib/device-auth.mjs`、`lib/auth-pages.mjs`、`lib/proxy.mjs`、`lib/web-rpc.js` 及对应测试。
2. 将 WebAuthn 公钥凭据模型替换为：
   - 设备 ID 与名称；
   - 设备 token 哈希；
   - 密码 Argon2id 哈希与盐；
   - 创建/最近使用时间；
   - 累计失败次数与等待截止时间；
   - 撤销状态与活动会话关联。
3. 旧 Passkey 凭据不能安全自动迁移为新密码凭据；升级后失败关闭，要求用户删除/重新配对，不增加兼容旁路。
4. 实现配对申请、电脑批准、设备 Cookie 下发与轮换。
5. 实现密码登录、累计退避、短会话、撤销与持久化。

### 第二阶段：替换浏览器流程

1. 将手机配对页改为“设备名称＋至少 6 位密码”。
2. 登录页只显示该设备的密码输入框和统一错误信息。
3. 不向用户显示设备 token、密码哈希或技术术语。
4. 正常刷新不重复认证；后台超过 10 分钟后返回重新认证。
5. 清除 Cookie/换浏览器后显示“此浏览器尚未配对”，只能回电脑重新配对。

### 第三阶段：更新电脑设置页

1. 保留“添加设备”、待批准申请、允许/拒绝、已批准设备、撤销设备。
2. 待批准申请显示设备名和提交时间。
3. 不显示明文密码，不提供远程找回或重置密码按钮。
4. 删除 Passkey、WebAuthn、指纹、面容等不再适用的界面文案。

### 第四阶段：依赖、构建与测试

1. 移除 SimpleWebAuthn 浏览器/服务端依赖及相关生成包。
2. 选择适合 Node.js 22 和 DSH 插件跨平台安装的成熟 Argon2id 实现；先核实 Windows/Linux 打包与安装兼容性，再决定具体依赖，不要问用户选库。
3. 调整 `client/build.mjs` 和生成产物命名，避免继续使用误导性的 `passkey.js`。
4. 重写认证测试，至少覆盖：
   - 配对 secret 一次性与 5 分钟失效；
   - 手机提交后不能自动进入白名单；
   - 本机允许/拒绝；
   - token 只存哈希、Cookie 属性正确、登录后轮换；
   - 密码只存慢哈希；
   - 未配对浏览器即使知道密码也不能登录；
   - 失败次数第 5/10/15/20 次的精确等待规则；
   - 成功后计数清零；
   - 重启后失败计数和等待截止时间仍存在；
   - 新会话和后台 10 分钟重新认证，刷新不重新认证；
   - 设备撤销后 HTTP、API 和 WebSocket 失效；
   - 未认证 HTTP/API/WS 继续失败关闭；
   - 错误消息与时序不泄露设备是否存在；
   - 清除浏览器凭据后必须重新配对；
   - 旧 PIN/LAN/Quick Tunnel/无认证 CLI 仍没有恢复。
5. 执行：

   ```powershell
   npm run build:client
   npm test
   npm audit --omit=dev
   npm pack --dry-run --json
   git diff --check
   ```

### 第五阶段：同步文档与真实验收

代码和测试完成后同步更新：

- `README.md`；
- `SECURITY.md`；
- `待用户处理.md`；
- `CHANGELOG.md`；
- `交接指南.md`；
- `package.json` 的 description、keywords、依赖与包内容；
- 所有 Passkey/WebAuthn 过时说明和测试名称。

然后重新部署当前本地 link 插件并做荣耀 50 真实验收。部署和重启 DSH Desktop 前必须先向用户说明并获得明确确认。

---

## 6. 真实环境事实

- 当前电脑：公司电脑 `DIKA-WORK-LAPTO`，Windows 11。
- 固定域名：`https://dsh-pocket-addin-work.zingerplayground.top`。
- Cloudflare Service：`http://127.0.0.1:3081`。
- 受管 `cloudflared`：`C:\Users\Zinger\.dsh\dsh-pocket\bin\cloudflared.exe`，版本 2026.8.3。
- 设置文件：`C:\Users\Zinger\.dsh\dsh-pocket\settings.json`（含 Tunnel Token，不得输出或提交）。
- 当前插件已通过 `link:`/Junction 接入 DSH Desktop profile，源码改动在 DSH 重启后生效；不要擅自替换 profile 或部署。
- 手机：荣耀 50；Passkey 平台认证器不可用，但浏览器、固定域名和公网配对页访问正常。
- 公司网络代理：`127.0.0.1:17891`。
- PowerShell 可能输出 `Cannot create type. Only core types are supported in this language mode.`，是当前 DSH 受限语言环境噪声，不是项目错误。

---

## 7. 已知安全边界

1. 通过认证的手机拥有完整 DSH 能力，相当于能够操作主机上的 DSH 权限。
2. “Pocket 设置仅本机显示”仍不是密码学权限边界：DSH RPC 的 loopback authority 无法区分代理后的远程请求。不要误报为严格本机隔离。
3. 任何纯浏览器方案都无法防御：
   - 同源 XSS 在页面内直接代替用户操作；
   - root、特权恶意软件或浏览器 profile 被完整复制；
   - 电脑本机或 Cloudflare 账号已经失陷。
4. 设备 Cookie 为 bearer 凭据；`HttpOnly` 防脚本读取，但不防已控制浏览器/系统的攻击者。设备密码和服务端限速是第二层保护。
5. 固定公网端点可被扫描；应用端限速与 Cloudflare 边缘防护都需要保留。
6. 允许逐步等待而非永久锁定会留下小范围可用性攻击：知道有效设备标识且能通过设备凭据校验的攻击者可触发最长 24 小时等待。不要扩大为按 IP 锁定，避免共享出口误伤。

---

## 8. 不要做的事情

- 不要继续尝试通过 WebAuthn 参数修复荣耀 50。
- 不要保留 Passkey 作为并列备用认证，除非用户以后明确改变业务决策；当前目标是一个中国 Android 可用的统一纯浏览器流程。
- 不要恢复 LAN、PIN 直达、`?token=`、30 天免登录、Quick Tunnel、无认证 CLI 或第三方 cloudflared 镜像。
- 不要创建用户名账号系统、邮箱/短信找回或远程恢复旁路。
- 不要保存或显示明文设备密码。
- 不要把设备 token 放 URL query/path 或日志。
- 不要把 Tunnel Token、Cloudflare 密码、真实密码或浏览器凭据写入仓库、测试快照或聊天。
- 不要为了兼容旧 Passkey 数据增加降低安全边界的自动迁移。
- 不要运行 `npm audit fix --force`。
- 不要擅自推送、提交、开 PR、发布、部署或重启正在使用的 DSH profile。
- 不要向用户询问技术选型；只在真正影响业务流程和人类使用体验时提问。

---

## 9. 当前未提交文件

本次会话只修改了文档，没有修改代码：

```text
M docs/dsh-pocket安全改造设计.md
M 交接指南.md
```

`docs/dsh-pocket安全改造设计.md` 已完整记录新方案；`交接指南.md` 即本文件。提交前仍需按多 AI 协作纪律检查当前分支与工作树，提交后核对提交落点。

---

## 10. 新会话可直接复制的开场指令

```text
继续 DSH Pocket 中国 Android 兼容认证改造。源码在 D:\Github\dsh-pocket。

先完整阅读：
1. D:\Github\dsh-pocket\交接指南.md
2. D:\Github\dsh-pocket\docs\dsh-pocket安全改造设计.md

然后 git fetch --all --prune，核对当前分支、HEAD 和未提交文档，但不要推送、提交、部署或修改正在使用的 DSH profile。

当前代码仍是 Passkey/WebAuthn；已确认的新目标是纯浏览器的“电脑批准过的设备凭据＋每设备独立密码”，覆盖中国 Android，不依赖 Google/WebAuthn/短信/邮箱/App。只和我讨论业务流程；技术实现按交接指南中的业界常用基线自行决定。先报告实施计划，再开始改代码。
```
