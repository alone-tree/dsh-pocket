# DSH Pocket 安全改造交接指南

> 交接时间：2026-09-01 09:31（Asia/Shanghai）  
> 接手对象：新会话中的 AI  
> 源码唯一位置：`D:\Github\dsh-pocket`  
> 原始设计文档：`D:\HermesSync\DeepSeekHarness\dsh-pocket安全改造设计.md`

## 0. 新会话先做什么

1. 把工作目录切到 `D:\Github\dsh-pocket`，不要在 `D:\HermesSync\DeepSeekHarness` 下创建源码副本。
2. 先完整阅读本文件，再读：
   - `README.md`
   - `SECURITY.md`
   - `待用户处理.md`
   - `CHANGELOG.md` 顶部“未发布：安全架构改造”
3. 按用户的 GitHub 协作规则同步远端信息，但不要改变当前分支内容：

   ```powershell
   git fetch --all --prune
   git branch --show-current
   git log -2 --oneline --decorate
   git status --short
   git remote -v
   ```

4. 应看到当前功能分支为 `feat/secure-passkey-access`。安全改造的代码基线提交是：

   ```text
   c4c36b9 feat: secure pocket access with passkeys
   ```

5. 本交接文档会在该基线之后作为单独提交存在。接手时以实际 `HEAD` 为准，不要把 `c4c36b9` 误认为包含本文件的提交。
6. 当前阶段不要推送、开 PR、发布 npm、部署或替换正在使用的 DSH 插件，除非用户再次明确授权。

---

## 1. 项目目标

原项目 `shaobeichen/dsh-pocket` 会把电脑上的 DeepSeek Harness 通过 LAN 或 Cloudflare Tunnel 暴露给手机。安全审计发现旧版不适合作为长期公网入口，主要问题包括：

- 默认监听 `0.0.0.0:3081`；
- LAN、公网和 Host 分类共同决定认证路径，边界复杂；
- 8 位 PIN、`?token=<PIN>` 直达和长期 Cookie；
- Quick Tunnel 随机域名；
- `dsh-pocket --public` 无认证 CLI；
- 自动从第三方镜像下载 `cloudflared`，没有校验签名或摘要；
- 代理会把远程 Host/Origin 改写成 loopback，DSH 宿主无法区分真实本机调用和经代理的远程调用。

本分支的目标是做一个个人自用、固定入口、以安全优先的替代版本：

- 每台 DSH 主机只有一个固定 HTTPS 地址；
- 公司、家里和移动网络都使用同一个地址，不再提供 LAN 入口；
- 只允许本机批准过的 Passkey/WebAuthn 设备；
- 每次打开或刷新页面重新做设备身份验证；
- 页面前台持续可用，退到后台超过 10 分钟失效；
- 不提供远程恢复、备用密码或兼容旧危险模式。

---

## 2. 已确认且不要重新讨论的产品决策

除非用户主动改变设计，接手会话不要再次询问以下问题：

1. **固定网址**：每台主机一个固定 HTTPS URL。
2. **无 LAN 入口**：代理仅监听 `127.0.0.1`，所有网络都通过固定公网 URL。
3. **Named Tunnel only**：只支持 Cloudflare Named Tunnel，不支持 Quick Tunnel。
4. **Passkey only**：浏览器身份使用 Passkey/WebAuthn，不提供账号密码和 PIN。
5. **配对必须双端确认**：
   - 电脑本机生成一次性二维码；
   - 手机完成人脸、指纹或设备 PIN；
   - 电脑本机仍需手动“允许”。
6. **设备命名**：配对时由用户填写设备名称。
7. **主机相互独立**：不同电脑的凭据、设备名单和会话互不共享。
8. **每次顶层导航重新验证**：重新打开、刷新或新标签页都必须重新调用 WebAuthn。
9. **会话规则**：页面前台每 30 秒续期；后台超过 10 分钟失效。
10. **无远程恢复**：全部凭据丢失后，只能回电脑本机重新配对。
11. **允许 Passkey 同步**：Apple/Google 账号把 Passkey 同步到用户自己的其他设备是可接受行为。
12. **手机权限**：验证通过后拥有完整 DSH 使用权。
13. **Pocket 安全管理**：界面上只在电脑本机显示；现有 DSH 权限模型无法把它做成严格不可绕过的本机权限边界，详见“已知限制”。
14. **安全优先于兼容**：可以删除危险功能，不要求与上游旧行为兼容。
15. **Cloudflare 密码**：插件永远不获取 Cloudflare 账号密码，只保存 Tunnel Token。

---

## 3. 仓库与 Git 状态

### 本地仓库

```text
D:\Github\dsh-pocket
```

### 远端

```text
origin   https://github.com/alone-tree/dsh-pocket.git
upstream https://github.com/shaobeichen/dsh-pocket.git
```

### 基线

- 上游基线：`main @ 8fc6bce`，上游版本 `v2.10.0`。
- 当前功能分支：`feat/secure-passkey-access`。
- 安全改造代码提交：`c4c36b9`。
- 当前功能分支尚未推送到 `origin`。
- 没有 PR、npm 发布或部署。
- `package.json` 已加入 `"private": true`，防止仍使用上游包名 `dsh-pocket` 时误发布 npm。

### 多 AI 协作注意

用户常同时使用两个 AI。任何提交前必须：

```powershell
git branch --show-current
git status --short
```

提交后必须：

```powershell
git log -1 --oneline --decorate
git status --short
```

不要覆盖其他 AI 的并行结论；若出现不同方案，在文档中并列并标记“待决定”。

---

## 4. 当前实现总览

### 4.1 固定公网入口

- `lib/service.mjs` 只启动 Named Tunnel。
- 固定域名或 Tunnel Token 缺失时，`startTunnel()` 失败关闭，不会创建 Quick Tunnel。
- 代理通过 `createPocketProxy()` 固定绑定 `127.0.0.1`。
- Cloudflare Public Hostname 的 Service 应指向：

  ```text
  http://127.0.0.1:3081
  ```

- 端口冲突时仅在 loopback 上尝试后续端口；若实际端口变化，Cloudflare ingress 也必须同步修改。

### 4.2 cloudflared 信任边界

`lib/tunnel.mjs` 只接受：

1. `DSH_POCKET_CLOUDFLARED` 指定且存在的路径；或
2. 系统 `PATH` 中已安装的 `cloudflared`。

找不到时直接报错。已删除：

- GitHub/第三方镜像自动下载；
- `ghproxy.net`、`gh.ddlc.top`、`gh-proxy.com`；
- Quick Tunnel；
- `trycloudflare.com` URL 解析；
- 本地缓存自动命中与下载逻辑。

Named Tunnel 使用固定参数：

```text
--no-autoupdate tunnel run --protocol http2
```

Tunnel Token 通过 `TUNNEL_TOKEN` 环境变量传给子进程，不进入命令行参数。

### 4.3 Passkey/WebAuthn 凭据

核心文件：`lib/device-auth.mjs`

生产依赖：

- `@simplewebauthn/server@13.3.3`
- `@simplewebauthn/browser@13.3.0`

固定约束：

- Origin 必须是无路径的 HTTPS Origin；
- RP ID 是固定域名 hostname；
- 注册与登录均要求 `userVerification: 'required'`；
- 已批准凭据通过 `allowCredentials` 白名单传入；
- WebAuthn 挑战 2 分钟失效，使用一次后删除；
- 匿名登录挑战表最多 256 条，超出时淘汰最旧挑战，避免内存无界增长；
- 每个凭据最多保留 16 个活动会话，全局最多 256 个活动会话。

持久化文件：

```text
$DSH_HOME/dsh-pocket/device-credentials.json
```

只持久化：

- credential ID；
- 公钥；
- counter；
- transports；
- device type / backup state；
- 设备名称；
- 创建时间和最近使用时间。

只在内存保存：

- 配对 secret；
- 注册和登录 challenge；
- 待批准申请；
- 登录 session；
- 单次入口 ticket。

### 4.4 配对流程

1. 电脑设置页通过 RPC `device.pairingStart` 调用 `beginPairing()`。
2. 生成 5 分钟有效的 `id.secret`，放在二维码 URL fragment：

   ```text
   https://固定域名/pocket-pair#pair=<id.secret>
   ```

   fragment 不会自动发送给服务器，由浏览器脚本读取后放入 POST JSON。
3. 手机打开 `/pocket-pair`，填写设备名称。
4. `/pocket-pair/options` 生成 WebAuthn 注册参数。
5. 手机完成用户验证后，`/pocket-pair/verify` 校验注册响应。
6. 凭据先进入内存 `pending`，不会自动加入白名单。
7. 电脑本机设置页显示待批准设备；点击“允许”后才写入凭据文件。
8. 二维码使用一次后立即失效；待批准申请也会在 5 分钟后失效。

### 4.5 登录与每次刷新重新验证

1. 未带一次性入口 ticket 的顶层 HTML 导航会显示 Passkey 登录页。
2. `GET /pocket-auth/options` 生成登录 challenge。
3. 浏览器调用 `startAuthentication()`。
4. `POST /pocket-auth/verify` 校验白名单凭据和用户验证。
5. 成功后设置 Session Cookie：

   ```text
   __Host-dsh_pocket_device_session=<随机值>;
   Secure; HttpOnly; SameSite=Strict; Path=/
   ```

   不设置长期 `Max-Age`。
6. 同时生成一次性 `pocket-entry` ticket，浏览器跳转至根页面。
7. 代理消费 ticket 后才允许这一次顶层 HTML 进入 DSH。
8. ticket 在转发上游前由 `stripPocketEntry()` 删除，不进入 DSH URL 或日志。
9. HTML 响应注入 `SESSION_GUARD_SCRIPT`：
   - 删除地址栏中的 ticket；
   - 页面可见时每 30 秒调用 `/pocket-auth/keepalive`；
   - 页面再次可见时立即续期；
   - 收到 401 后回到登录页。
10. 顶层 HTML 请求会删除上游 `accept-encoding`，确保 HTML 未压缩并能可靠注入守卫。
11. 代理给认证页设置 CSP、`frame-ancestors 'none'`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer` 和 `nosniff`。

### 4.6 10 分钟后台失效

- session 只根据 `lastForegroundAt` 判断；普通 API 请求不会延长它。
- 页面可见时 keepalive 更新 `lastForegroundAt`。
- 页面后台不再 keepalive，超过 10 分钟后 session 从内存删除。
- WebSocket 建立时验证 session，随后每 30 秒复查；过期或撤销后销毁 socket。
- 设备撤销时会删除该 credential 对应的全部 session。

### 4.7 设置页

核心文件：`client/index.jsx`

设置页只保留：

- 固定 Hostname；
- Tunnel Token（只写不回显）；
- 开启/关闭固定公网入口；
- 生成配对二维码；
- 查看待批准申请；
- 允许/拒绝申请；
- 查看和撤销设备。

当页面 Origin 等于固定公网 Origin 时，只显示“安全设置只能在电脑本机管理”，不渲染管理控件。这是界面防误操作，不是严格权限隔离。

### 4.8 代理和 DSH 兼容逻辑

`lib/proxy.mjs` 仍负责：

- Host/Origin 改写为 DSH loopback authority；
- HTTP 与 WebSocket 双向透传；
- DSH 启动 token 首次根路径握手；
- 清理历史 `dsh-desktop-*` URL 参数；
- HTML 注入；
- 大 JSON/text gzip/brotli；
- 上游 DSH Desktop `403 forbidden` 导航提示；
- WebSocket 心跳与连接清理。

旧 PIN、Host 分类、LAN、`?token=`、30 天 Cookie 代码已经从该文件删除，不是简单隐藏。

---

## 5. 已删除的危险入口

以下功能已从发布路径和测试中删除：

- `bin/dsh-pocket.mjs`；
- `package.json` 的 `bin` 字段；
- `files` 中的 `bin`；
- `dsh-pocket --public`；
- `0.0.0.0` 默认监听；
- LAN URL、LAN QR、LAN 开关和 LAN IP 选择；
- 公网 PIN、LAN PIN、自定义 PIN、临时 PIN；
- `?token=<PIN>` 绕过；
- 长期 30 天 PIN Cookie；
- Quick Tunnel；
- 第三方镜像和自动下载；
- `qrcode-terminal` 直接依赖；
- 旧 `test/auth-routing.test.js`。

源码扫描命令：

```powershell
rg "startQuickTunnel|trycloudflare\.com|pocket-login|dsh_pocket_token|token\.lanRefresh|pin\.setCustom|host = '0\.0\.0\.0'|--public|ghproxy|gh\.ddlc|gh-proxy" .
```

README 和“确保不存在”的测试中会出现上述词语，这是说明或否定断言，不是活动代码。

---

## 6. 文件地图

| 文件 | 当前作用 |
|---|---|
| `README.md` | 当前安全分支的产品说明、准备条件、使用流程和重要边界 |
| `SECURITY.md` | 威胁模型、保护对象、公开端点、核心控制和已知限制 |
| `待用户处理.md` | Cloudflare、官方 cloudflared、DSH Desktop 和手机端到端验收清单 |
| `CHANGELOG.md` | 顶部记录本次未发布安全改造 |
| `package.json` | 新依赖、移除 CLI、`private: true`、发布文件白名单 |
| `lib/index.js` | Cordis 插件入口，初始化设置、设备认证、服务和 RPC |
| `lib/device-auth.mjs` | WebAuthn 凭据、配对、批准、挑战、短会话、撤销与持久化 |
| `lib/auth-pages.mjs` | 登录/配对 HTML 和页面前台 keepalive 守卫 |
| `lib/proxy.mjs` | loopback 反向代理、认证边界、HTTP/WS 转发和 DSH 兼容逻辑 |
| `lib/service.mjs` | loopback 代理和 Named Tunnel 生命周期、自动恢复状态 |
| `lib/tunnel.mjs` | 解析官方 cloudflared、启动 Named Tunnel，不含下载和 Quick Tunnel |
| `lib/settings.mjs` | 固定域名、Tunnel Token、proxy port、cloudflared path |
| `lib/web-rpc.js` | Tunnel 与设备管理 RPC，以及保留的 DSH 辅助 RPC |
| `client/passkey-entry.js` | 浏览器 Passkey 登录与配对流程 |
| `client/passkey.js` | `passkey-entry.js` 的生成产物 |
| `client/index.jsx` | 电脑本机设置页 |
| `client/client.js` | 设置页与移动适配生成产物 |
| `client/build.mjs` | 构建 `client.js` 和 `passkey.js` |
| `test/device-auth.test.js` | 凭据、配对、批准、过期、撤销、持久化和挑战上限 |
| `test/secure-proxy.test.js` | 未认证 HTTP/API/WS、单次入口、公开配对页、CSP |
| `test/proxy.test.js` | 当前代理核心回归测试，不再含 PIN/LAN 旧测试 |
| `test/service.test.js` | loopback、Named Tunnel、失败关闭、RPC、端口回退 |
| `test/download.test.js` | 只接受官方/显式 cloudflared，禁止下载回退 |
| `test/settings.test.js` | 精简后的安全设置持久化测试 |
| `test/smoke.test.js` | 真实代理、浏览器包和危险入口删除检查 |
| `test/tunnel-args.test.js` | Named Tunnel 参数和 Quick Tunnel 删除检查 |

`client/client.js` 和 `client/passkey.js` 都是应提交的生成产物；改源文件后必须重新构建。

---

## 7. 验证结果

### 最后一次完整构建与测试

执行：

```powershell
npm run build:client
npm test
```

结果：

```text
68 tests
68 pass
0 fail
0 skipped
```

覆盖了：

- Passkey 注册和认证；
- 手机申请不能自动进入白名单；
- 本机批准、拒绝、撤销；
- 10 分钟失效；
- 一次性配对和入口 ticket；
- 挑战表内存上限；
- 未认证 HTTP/API/WS 拒绝；
- ticket 不进入 DSH 上游；
- 登录页 CSP 和防 iframe；
- loopback 监听；
- Named Tunnel only；
- 配置缺失失败关闭；
- 官方 cloudflared only；
- 危险模式和 CLI 删除；
- 原有移动端适配、文件复制、压缩、DSH token 和 WebSocket 兼容回归。

### 生产依赖审计

执行：

```powershell
npm audit --omit=dev --json
```

最后结果：

```text
生产依赖 54
漏洞 0
```

完整开发依赖审计仍报告：

```text
3 moderate
15 high
共 18 个
```

这些来自开发工具链（例如 semantic-release 相关依赖），不在生产依赖审计中。没有执行 `npm audit fix --force`，因为它可能破坏版本和构建链。

### 发布包检查

执行：

```powershell
npm pack --dry-run --json
```

检查结果：

- 包内不含 `bin/`；
- 包内包含 `README.md` 和 `SECURITY.md`；
- 包内包含 Passkey 浏览器包和设备认证模块；
- `package.json` 是 private，当前禁止误发布。

### 代码卫生

已执行：

```powershell
git diff --check
```

无 whitespace error。敏感信息模式扫描无命中。PowerShell 在当前 DSH 环境会反复输出：

```text
InvalidOperation: Cannot create type. Only core types are supported in this language mode.
```

这是受限语言环境噪声；只要命令退出码和实际输出正常，不是项目错误。

---

## 8. 已知限制与不可误报的结论

### 8.1 “安全设置仅本机管理”不是严格权限边界

这是最重要的限制。

DSH RPC 使用：

```js
{ authority: 'loopback' }
```

但 Pocket 代理把公网请求的 Host/Origin 改写为 loopback。通过 Passkey 的手机又被设计为拥有完整 DSH 权限。因此，远程用户理论上可通过完整 DSH 能力修改主机文件、插件设置或调用宿主能力。

当前实现只能做到：

- 公网设置页不显示 Pocket 管理控件；
- 降低误操作概率；
- 不主动提供远程设备管理入口。

不能声称：

- 已从机制上证明远程用户绝对无法改 Pocket 设置；
- `authority: loopback` 能区分本机与经代理远程请求。

彻底解决需要 DSH 宿主提供不可由反向代理伪造的本地调用身份或独立权限域。

### 8.2 尚未做真实端到端验收

自动测试全部通过，但缺少：

- 真实 Cloudflare Named Tunnel；
- 真实固定域名；
- 真实 Tunnel Token；
- iPhone/Android Passkey；
- Apple/Google Passkey 同步；
- 真实 DSH Desktop 页面与 WebSocket 长连接；
- 前台 10 分钟与后台 10 分钟实测；
- 撤销后真实浏览器 WS 约 30 秒关闭；
- DSH 重启后的真实凭据保留与会话失效。

不能把自动测试通过表述为已完成真实手机验收。

### 8.3 Cloudflare 入口与 DoS

- 固定公网端点可被扫描。
- 代码已给 WebAuthn challenge 表设置 256 条硬上限，避免内存无界增长。
- 持续请求仍可能造成可用性影响；建议在 Cloudflare 侧给固定 Hostname 配置基础速率限制。
- 不要自行在应用层增加复杂账户系统或多源兜底；用户偏好先修正源头和保持简单。

### 8.4 Passkey 的实际提示由设备控制

代码每次顶层导航都会重新调用 WebAuthn，且要求 `userVerification: required`。具体显示指纹、面容还是设备 PIN，以及系统是否采用自身的短时验证策略，由浏览器和操作系统决定。不要承诺能控制操作系统的生物识别界面细节。

### 8.5 固定域名变更

WebAuthn 凭据绑定 Origin/RP ID。修改固定域名后，旧凭据无法在新域名使用，需要在电脑本机重新配对。这是安全属性，不应增加跨域迁移绕过。

---

## 9. 仍需用户完成的真实操作

完整清单在 `待用户处理.md`，核心步骤如下：

1. Cloudflare Zero Trust 创建 Named Tunnel。
2. 配置固定 Public Hostname。
3. Service 指向 `http://127.0.0.1:3081`（若实际代理端口改变，按实际值）。
4. 从 Cloudflare 官方渠道安装 `cloudflared`。
5. 在 DSH Desktop 开启浏览器访问并使用 compatibility 模式。
6. 在电脑设置页填 Hostname 和 Tunnel Token。
7. 开启固定入口。
8. 手机扫码配对并在电脑批准。
9. 按 `待用户处理.md` 的 11 步验证真实设备行为。

任何 Cloudflare 密码、验证码或 Passkey 生物信息都不应写入仓库、聊天或配置。

---

## 10. 下一会话推荐执行顺序

如果用户下一会话说“继续”，按以下顺序推进：

### 情况 A：用户准备做真实验收

1. 先读本文件和 `待用户处理.md`。
2. 确认用户当前在哪台电脑、使用哪个 DSH profile，不能从旧会话推断。
3. 只读检查本机是否已有官方 `cloudflared`、DSH Desktop 浏览器访问和 compatibility 模式。
4. Cloudflare 配置涉及账号和真实域名时，让用户本人完成必要登录/授权，不收集密码。
5. 不直接替换当前 DSH 插件；先说明本地安装/软链方案并取得明确确认。
6. 安装后按 11 步端到端验收逐项记录真实结果。
7. 任何实测发现都同步更新 `README.md`、`SECURITY.md`、`待用户处理.md` 或本文件。

### 情况 B：用户准备提交上游

1. 先决定提交形式：
   - 单个大 PR；
   - 设计/威胁模型 issue；
   - 拆分 PR；
   - 仅维护独立 fork。
2. 不要擅自替用户决定。
3. 上游 PR 前需要评估：
   - `private: true` 是否移除；
   - README 是否改回上游兼容叙述；
   - 版本和 package metadata；
   - 删除大量旧功能是否适合单个 PR；
   - 是否保留旧 API 迁移提示。
4. 推送和开 PR 都必须再次获得用户明确确认。

### 情况 C：用户准备独立 fork 自用或发布

1. 先决定新包名和版本，避免覆盖上游 npm 包。
2. 修改 `repository`、`bugs`、`homepage` 和发布流程。
3. 保留 GPL-2.0、原作者版权和移动端 MIT 许可。
4. 只有用户明确要求发布后，才移除 `private: true`。

---

## 11. 不要做的事情

- 不要把源码复制到 `D:\HermesSync\DeepSeekHarness`。
- 不要恢复 LAN、PIN、Quick Tunnel 或无认证 CLI 来换取便利。
- 不要重新加入第三方镜像自动下载。
- 不要运行 `npm audit fix --force`。
- 不要声称远程安全管理已形成严格本机边界。
- 不要声称真实手机端到端已通过。
- 不要推送、开 PR、发布或部署，除非用户明确确认。
- 不要直接修改正在运行的 DSH profile 做试验。
- 不要把 Tunnel Token、Passkey 数据、Cloudflare 密码或真实域名秘密提交到 Git。
- 不要在未重新构建的情况下提交 `client/index.jsx` 或 `client/passkey-entry.js` 的改动。

---

## 12. 新会话可直接复制的开场指令

```text
继续 DSH Pocket 安全改造项目。源码在 D:\Github\dsh-pocket。

请先完整阅读：
1. D:\Github\dsh-pocket\交接指南.md
2. D:\Github\dsh-pocket\README.md
3. D:\Github\dsh-pocket\SECURITY.md
4. D:\Github\dsh-pocket\待用户处理.md

然后同步远端信息并核对当前分支、HEAD 和工作树，但不要推送、开 PR、发布、部署或修改正在使用的 DSH profile。当前目标以交接指南为准；不要恢复 LAN、PIN、Quick Tunnel、无认证 CLI 或第三方 cloudflared 自动下载。先报告你读取到的当前状态，再按我下一条指令继续。
```

---

## 13. 当前完成定义

本轮编码工作已经完成：

- 设计目标已实现；
- 危险入口已删除；
- 核心文档已更新；
- 构建通过；
- 自动测试 68/68 通过；
- 生产依赖审计 0 漏洞；
- 发布包不含 CLI；
- 安全改造已在本地功能分支提交。

下一阶段的唯一硬缺口是真实 Cloudflare + 真实手机 + 真实 DSH Desktop 端到端验收，以及用户随后选择独立 fork 或上游提交路线。
